<Window x:Class="Examine2DView.Dialogs.AutoExternalBoundaryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Auto External Boundary" 
        Height="280" 
        Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Description -->
        <TextBlock Grid.Row="0" 
                   Text="Automatically create a rectangular external boundary based on existing geometry extents."
                   TextWrapping="Wrap"
                   Margin="0,0,0,15"/>

        <!-- Settings Panel -->
        <GroupBox Grid.Row="1" Header="Boundary Options" Padding="10">
            <StackPanel>
                <!-- Expansion Factor -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" 
                               Text="Expansion Factor:"
                               VerticalAlignment="Center"/>
                    <Slider Grid.Column="1" 
                            x:Name="ExpansionFactorSlider"
                            Minimum="1.1"
                            Maximum="5.0"
                            Value="2.0"
                            TickFrequency="0.1"
                            IsSnapToTickEnabled="True"
                            VerticalAlignment="Center"
                            Margin="5,0"/>
                    <TextBlock Grid.Column="2" 
                               Text="{Binding ElementName=ExpansionFactorSlider, Path=Value, StringFormat='{}{0:F1}x'}"
                               VerticalAlignment="Center"
                               Width="45"
                               TextAlignment="Right"/>
                </Grid>

                <!-- Min Margin -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" 
                               Text="Minimum Margin:"
                               VerticalAlignment="Center"/>
                    <Slider Grid.Column="1" 
                            x:Name="MinMarginSlider"
                            Minimum="0"
                            Maximum="50"
                            Value="5"
                            TickFrequency="0.5"
                            IsSnapToTickEnabled="True"
                            VerticalAlignment="Center"
                            Margin="5,0"/>
                    <TextBlock Grid.Column="2" 
                               Text="{Binding ElementName=MinMarginSlider, Path=Value, StringFormat='{}{0:F1}m'}"
                               VerticalAlignment="Center"
                               Width="45"
                               TextAlignment="Right"/>
                </Grid>

                <!-- Description -->
                <TextBlock Text="The external boundary will be sized to either the expansion factor or minimum margin, whichever is larger."
                           TextWrapping="Wrap"
                           FontSize="11"
                           Foreground="Gray"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="OK" 
                    Width="80" 
                    Height="30" 
                    Margin="0,0,10,0"
                    IsDefault="True"
                    Click="OkButton_Click"/>
            <Button Content="Cancel" 
                    Width="80" 
                    Height="30"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
